%nav.top-bar
  %ul.title-area
    %li.name
      %h1= link_to "RPG Tools | Gestionnaire de PNJ", root_path
  %section.top-bar-section
    - if logged_in?
      = render partial: "shared/nav_logged_in"
    - else
      = render partial: "shared/nav_logged_out"
